//---------------------------------------------------------------------------
#include "../Connection.h"

#ifndef bluetoothH
#define bluetoothH
//---------------------------------------------------------------------------
#endif
class ConnectionBluetooth : public Connection{

	public:

		void connection(){

		};

		int readData(char* buf, int size){

		};
}
